// var, let e const
function iniciarTime(iniciaJogo: boolean){
  var nome:string = 'Time De Amigos';
  let estado:string  = 'DF';
  const cidade:string = 'BSB';
  console.log(`${nome} - ${estado} - ${cidade} `);
}

iniciarTime(false);